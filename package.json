{
  "name": "ai_pulse_analytics",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "bestzip build.zip * --ignore=node_modules/aws-sdk/* --ignore=.git",
    "deploy": "npm run build && echo 'Deploying zip file...'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "bestzip": "^2.2.1"
  },
  "dependencies": {
    "mongoose": "^8.10.1",
    "@aws-sdk/client-kinesis": "^3.750.0"
  }
}
